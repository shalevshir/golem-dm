node_modules/
dist/
coverage/
tools/sim/runs/
.env
.env.*
!.env.example
*.log
.DS_Store

# Visual brainstorming companion sessions (mockups, ephemeral)
.superpowers/brainstorm/

# Claude Code local state. `worktrees/` holds full checkouts of other
# branches — 769M at the time this was added, and the reason a root
# `pnpm lint` walks sibling worktrees and reports their errors as ours.
.claude/
